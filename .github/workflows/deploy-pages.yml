# DISABLED — deployment is now handled by ci.yml (build -> test -> deploy only if tests pass).
# Kept as a manual-only no-op because this file could not be deleted automatically.
name: (disabled) Deploy Pages
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — see ci.yml"
