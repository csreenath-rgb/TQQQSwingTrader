name: Paper auto-rebalance (Alpaca)

# Optional. Does nothing unless the PAPER_WORKER_URL secret is set.
# Runs each weekday at 19:50 UTC (~15:50 ET EDT / ~14:50 ET EST). paper_rebalance.py gates by the
# strategy's rebalance mode (daily/signal every day; weekly on the new-week boundary; monthly on month
# change) so live rebalance timing matches the backtest chart.
on:
  schedule:
    - cron: "50 19 * * 1-5"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: paper-rebalance
  cancel-in-progress: false

jobs:
  rebalance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compute target & rebalance paper account
        env:
          PAPER_WORKER_URL: ${{ secrets.PAPER_WORKER_URL }}
          PAPER_ACCESS_TOKEN: ${{ secrets.PAPER_ACCESS_TOKEN }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:   ${{ secrets.TELEGRAM_CHAT_ID }}
          SMTP_HOST:          ${{ secrets.SMTP_HOST }}
          SMTP_PORT:          ${{ secrets.SMTP_PORT }}
          SMTP_USER:          ${{ secrets.SMTP_USER }}
          SMTP_PASS:          ${{ secrets.SMTP_PASS }}
          ALERT_TO:           ${{ secrets.ALERT_TO }}
        run: python alerts/paper_rebalance.py
